name: background_worker_pkg
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.27.7
  combine: ^0.5.6

  # Modules
  dependency_interfaces:

dev_dependencies:
  flutter_test:
    sdk: flutter
